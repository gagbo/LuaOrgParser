-- Abstract Syntax Table for org-mode syntax
local ast = {}

-- TODO : Add the node -> children structure
-- TODO : Add an accessor for the "current element" of the tree ? Or just construct a small AST recursively anyway ?

return ast
